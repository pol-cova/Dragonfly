# Next.js (apps/web) — required for production
NEXT_PUBLIC_CONVEX_URL=

# Optional — not used by claim UX today (local referee path)
NEXT_PUBLIC_MIDNIGHT_NETWORK=undeployed
NEXT_PUBLIC_MIDNIGHT_CONTRACT_ADDRESS=

# Convex CLI (local dev — set by `npx convex dev`)
CONVEX_DEPLOYMENT=

# Midnight contract package (optional local tooling)
MIDNIGHT_NETWORK=undeployed
